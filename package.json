{
  "name": "micro-services",
  "version": "1.0.0",
  "description": "micro-service",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "pm2 start ecosystem.config.js --update-env",
    "stop": "pm2 stop ecosystem.config.js",
    "logs": "pm2 logs",
    "reset": "pm2 delete ecosystem.config.js"
  },
  "author": "paul-gaultier",
  "license": "ISC",
  "dependencies": {
    "ejs": "^3.1.9",
    "swagger-ui-dist": "^5.11.8"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
